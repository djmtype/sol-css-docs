---
import MainNav from "@/components/Base/MainNav.astro"

type Props = {
  currentPage: string
}

const { currentPage } = Astro.props
---

<header>
  <div>
    <p style="font-size: var(--font-size-1)"><strong>SOL</strong> CSS</p>
    <MainNav currentPage={currentPage} />
  </div>
</header>
