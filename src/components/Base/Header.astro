---
import NavPrimary from "@/components/Base/NavPrimary.astro"
import Logo from "@/components/Base/Logo.astro"

type Props = {
  currentPage: string
}

const { currentPage } = Astro.props
---

<header style="position: relative; z-index: 1;">
  <div>
    <a href="/">
    <Logo style={{ blockSize: '2rem', inlineSize: 'auto' }} />
    <span class="sr-only">SOL CSS</span>
    </a>
    <NavPrimary currentPage={currentPage} />
  </div>
</header>
