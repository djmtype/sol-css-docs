---
import PrimaryNav from "@/components/Base/PrimaryNav.astro"
import Logo from "@/components/Base/Logo.astro"

type Props = {
  currentPage: string
}

const { currentPage } = Astro.props
---

<header>
  <div>
    <a href="/">
    <Logo style={{ blockSize: '2rem', inlineSize: 'auto' }} />
    <span class="sr-only">SOL CSS</span>
    </a>
    <PrimaryNav currentPage={currentPage} />
  </div>
</header>
