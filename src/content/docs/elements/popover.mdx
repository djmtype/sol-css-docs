---
title: "Popover"
description: "A popover element"
category: elements
order: 6
---
import Popover from "@/components/Popover.astro?raw";

A `<popover>` element can be used as a tooltip or non-modal, meaning that the rest of the page can be interacted with while the popover is being shown.

## Popover Navigation

<CodeComponent style={{placeContent: 'center'}} code={Popover} wrap />