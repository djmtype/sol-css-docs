---
title: "Dialog"
description: "A dialog element, sometimes referenced as a modal"
category: elements
order: 3
---
import Dialog from "@/components/Dialog.astro?raw";
import DialogForm from "@/components/DialogForm.astro?raw";

The `<dialog>` element is similar to a modal. This element requires javascript to function.

## Dialog (Basic)

<CodeComponent open style={{placeContent: 'center'}} code={Dialog} wrap />

## Dialog (Form)

<CodeComponent open style={{placeContent: 'center'}} code={DialogForm} wrap />

