---
title: "Tables"
description: "A tables element"
category: elements
order: 8
---
import Table from "@/components/Table.astro?raw";
import TableScroll from "@/components/TableScroll.astro?raw";

## Table (Default)

<CodeComponent wrap code={Table} />

## Table (Overflow)

Wrap a `<table>` within a `<figure>` for overflow scrolling.

<CodeComponent wrap code={TableScroll} />