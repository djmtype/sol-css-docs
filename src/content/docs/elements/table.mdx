---
title: "Table"
description: "The table element"
category: elements
order: 8
---
import Table from "@/components/Table.astro?raw";
import TableScroll from "@/components/TableScroll.astro?raw";

## Table (Default)

<CodeComponent open wrap code={Table} />

## Table (Overflow)

Wrap a `<table>` within a `<figure>` for overflow scrolling.

<CodeComponent open wrap code={TableScroll} />