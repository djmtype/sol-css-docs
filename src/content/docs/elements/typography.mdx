---
title: "Typography"
description: "Documentation for typographical elements including typescale, headings, lists, inline elements, and blockquote."
category: elements
order: 3
---
import HeadingsScript from "@/components/HeadingsScript.astro";
import Headings from "@/components/Headings.astro?raw";
import Typescale from "@/components/Typescale.astro";
import Blockquote from "@/components/Blockquote.astro?raw";
import Lists from "@/components/Lists.astro?raw";
import InlineText from "@/components/InlineText.astro?raw";
import ColorPalette from "@/components/Color/ColorPalette.astro";

## Overview

### Font Size

The default font size utilizes a `clamp()` function for better readability. Its ideal value is based on a ~1440px viewport.

```css
:where(html) {
   --font-size-default: clamp(1rem, 0.93rem + 0.35vw, 1.25rem);
}
```

### Font Scale

The default font scale is based on a `1.25` ratio, or *Major Third*.  
[Check out other type scales](https://typescale.com/)

```css
:where(html) {
   --font-scale-default: 1.25;
}
```

---

## Visual Typescale

Font size properties are defined using `--font-size-{00-6}`.

<Typescale />

---

## Text Elements

### Headings

<CodeComponent code={Headings} wrap class="flow" style={{ padding: "var(--spacing-panel)", border: "var(--border-width-hairline) solid var(--color-separator)", borderRadius: "var(--radius-panel)" }} />
<HeadingsScript />

### Lists

#### Unordered, Ordered, Definition

<CodeComponent code={Lists} wrap style={{display: 'grid', gap: 'var(--spacing-sibling) var(--spacing-gutter)', gridTemplateColumns: 'repeat(auto-fit, minmax(min(100%, 20ch), 1fr))', alignItems: 'baseline', justifyItems: 'start'}} />

### Inline Text Elements

<CodeComponent code={InlineText} wrap style={{display: 'grid', gap: 'var(--spacing-sm) var(--spacing-gutter)', gridTemplateColumns: 'repeat(auto-fit, minmax(min(100%, 20ch), 1fr))', alignItems: 'baseline', justifyItems: 'start'}} />

### Blockquote

<CodeComponent code={Blockquote} wrap />

